// Minimal Database type placeholder so @supabase/ssr's generics compile.
// Replace with the real generated types once your project is linked:
//   npx supabase gen types typescript --project-id <ref> > src/types/database.ts
// Until then, queries are typed loosely but the app remains fully functional.
export type Database = any;
